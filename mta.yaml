_schema-version: "3.2"
ID: travustspytravelproc
description: Generated by Fiori Tools
version: 0.0.1
modules:
- name: travustspytravelproc-destination-content
  type: com.sap.application.content
  requires:
  - name: travustspytravelproc-destination-service
    parameters:
      content-target: true
  - name: travustspytravelproc-repo-host
    parameters:
      service-key:
        name: travustspytravelproc-repo-host-key
  - name: travustspytravelproc-uaa
    parameters:
      service-key:
        name: travustspytravelproc-uaa-key
  parameters:
    content:
      instance:
        destinations:
        - Name: travustspytravelproc_html_repo_host
          ServiceInstanceName: travustspytravelproc-html5-srv
          ServiceKeyName: travustspytravelproc-repo-host-key
          sap.cloud.service: travustspytravelproc
        - Authentication: OAuth2UserTokenExchange
          Name: travustspytravelproc_uaa
          ServiceInstanceName: travustspytravelproc-xsuaa-srv
          ServiceKeyName: travustspytravelproc-uaa-key
          sap.cloud.service: travustspytravelproc
        existing_destinations_policy: ignore
  build-parameters:
    no-source: true
- name: travustspytravelproc-app-content
  type: com.sap.application.content
  path: .
  requires:
  - name: travustspytravelproc-repo-host
    parameters:
      content-target: true
  build-parameters:
    build-result: resources
    requires:
    - artifacts:
      - travustspytravelproc.zip
      name: travustspytravelproc
      target-path: resources/
- name: travustspytravelproc
  type: html5
  path: .
  build-parameters:
    build-result: dist
    builder: custom
    commands:
    - npm install
    - npm run build:cf
    supported-platforms: []
resources:
- name: travustspytravelproc-destination-service
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      HTML5Runtime_enabled: true
      init_data:
        instance:
          destinations:
          - Authentication: NoAuthentication
            Name: ui5
            ProxyType: Internet
            Type: HTTP
            URL: https://ui5.sap.com
          existing_destinations_policy: update
      version: 1.0.0
    service: destination
    service-name: travustspytravelproc-destination-service
    service-plan: lite
- name: travustspytravelproc-uaa
  type: org.cloudfoundry.managed-service
  parameters:
    path: ./xs-security.json
    service: xsuaa
    service-name: travustspytravelproc-xsuaa-srv
    service-plan: application
- name: travustspytravelproc-repo-host
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-name: travustspytravelproc-html5-srv
    service-plan: app-host
parameters:
  deploy_mode: html5-repo
  enable-parallel-deployments: true
